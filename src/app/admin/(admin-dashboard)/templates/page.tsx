import CmsPagesManager from "@/views/admin/CmsPagesManager";

export const metadata = {
  title: "Template Gallery | Store Admin",
};

export default function TemplatesPage() {
  return <CmsPagesManager />;
}
